import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.jsx';

// Here is where we attach the react to the HTML so it knows where to render
ReactDOM.render(<App />, document.getElementById('root'));
